@*
    /// ============================================================
    /// Author: Shaun Curtis, Cold Elm Coders
    /// License: Use And Donate
    /// If you use it, donate something to a charity somewhere
    /// ============================================================
*@

@implements IDisposable
@namespace Blazr.App.UI
@inherits BlazrAppPagedListForm<DboWeatherLocation, WeatherLocationEntity>

<UIListControl TRecord=DboWeatherLocation Records=this.Service.Records LoadState=this.loadState class="@this.TableCss">
    <RowTemplate>
        <UIListColumn TRecord=DboWeatherLocation HeaderTitle="ID" SortField="Uid" NoWrap=true>@context.Uid.AsShortGuid()</UIListColumn>
        <UIListColumn TRecord=DboWeatherLocation HeaderTitle="Location" SortField="Location" IsMaxColumn=true>@context.Location</UIListColumn>
        <UIListColumn TRecord=DboWeatherLocation HeaderTitle="" NoWrap=true class="text-right text-end">
            @this.AuthorizedRowButtonMarkup(context, context.Uid, true)
        </UIListColumn>
    </RowTemplate>
</UIListControl>

@code {
}
