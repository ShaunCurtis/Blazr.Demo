@page "/weatherlocation"
@page "/weatherlocations"
@namespace Blazr.App.UI
@attribute [Authorize(Policy = "IsVisitorPolicy")]

<WeatherLocationPagedListForm RouteId=RouteId UseModalForms=true/>

@code {
    private static Guid RouteId = Guid.NewGuid();
}

