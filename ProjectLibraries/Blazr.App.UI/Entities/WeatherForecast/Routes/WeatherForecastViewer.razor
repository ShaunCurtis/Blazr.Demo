@page "/Weatherforecast/view/{Id:guid}"
@page "/weatherforecasts/view/{Id:guid}"
@attribute [Authorize(Policy = "IsVisitorPolicy")]
@namespace Blazr.App.UI

<WeatherForecastViewForm Id=this.Id />

@code {
    [Parameter] public Guid Id { get; set; }
}
